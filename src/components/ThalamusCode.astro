---
export interface Props {
  formula: string;
  className?: string;
}

const { formula, className = "" } = Astro.props;
---

<div class={`code-block ${className}`.trim()}>
  <pre><code>{formula}</code></pre>
</div>
